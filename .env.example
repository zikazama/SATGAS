# ===========================================
# SATGAS - Environment Configuration
# Scalable Agent-to-agent Task Generation & Automation Service
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# LLM Provider Configuration
# ===========================================
# Choose provider: "qwen" (local CLI) or "openai" (API)
LLM_PROVIDER=qwen

# ===========================================
# Qwen CLI Settings (when LLM_PROVIDER=qwen)
# ===========================================
# Command to run Qwen CLI (default: qwen)
QWEN_CLI_COMMAND=qwen

# Model name for Qwen (optional)
#QWEN_MODEL=Qwen/Qwen2-7B-Instruct

# Timeout in seconds (0 = wait until done)
QWEN_TIMEOUT=0

# ===========================================
# OpenAI API Settings (when LLM_PROVIDER=openai)
# ===========================================
# Your OpenAI API key (required for openai provider)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Model to use (default: gpt-4o-mini)
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Temperature for response creativity (0.0-2.0, default: 0.7)
OPENAI_TEMPERATURE=0.7

# Maximum tokens in response (default: 4096)
OPENAI_MAX_TOKENS=4096

# Custom API base URL (optional)
# Use this for Azure OpenAI, local LLM servers, or other compatible APIs
# Examples:
#   Azure: https://your-resource.openai.azure.com/
#   Local: http://localhost:1234/v1
#   Together AI: https://api.together.xyz/v1
#OPENAI_BASE_URL=
